export default () => {
  // const text = ["today", "tomorrow", "next week", "next month", "next year"];
  // return text[Math.floor(Math.random() * text.length)];

  return ["today", "tomorrow", "next week", "next month", "next year"];
};
